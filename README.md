# running-dashboard

Custom dashboard to display metrics aggregated via Strava APIs.

## Development

Backend APIs can be found in the backend directory, frontend components can be found under frontend directory.

Both can be ran within this repository with the following commands:

```
npm run frontend:dev
npm run backend:dev
```
