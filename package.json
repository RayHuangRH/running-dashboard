{
  "name": "running-dashboard",
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && venv\\Scripts\\activate && uvicorn main:app --reload",
    "format": "prettier --write .",
    "format:py": "cd backend && venv\\Scripts\\activate && black . && ruff check . --fix"
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
